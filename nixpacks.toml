[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "./node_modules/.bin/serve -s dist --listen tcp://0.0.0.0:$PORT"
