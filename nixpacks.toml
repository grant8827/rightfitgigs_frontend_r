[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

# No build step - dist is pre-committed, vite is in devDependencies

[start]
cmd = "./node_modules/.bin/serve -s dist --listen tcp://0.0.0.0:$PORT"
